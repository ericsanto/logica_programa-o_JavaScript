const x = parseFloat(prompt('Digite um número: '))

const y = parseFloat(prompt('Digite mais um número: '))

const soma = x + y
const subtracao = x - y
const multiplicacao = x * y
const divisao = x / y

alert(
  "Resultado:\n " +
  "\nSoma: " + soma +
  "\nA subtração é: " +  subtracao +
  "\nA multiplicação é: " + multiplicacao +
  "\nA divisão é: " + divisao 
)